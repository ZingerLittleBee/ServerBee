# 10 磁盘 I/O 监控 — 冒烟测试

**前置条件**:Agent 运行于 Linux/macOS(磁盘 IO 仅这两平台采集)。深度用例见 [../disk-io.md](../disk-io.md)。

| # | 测试场景 | 操作步骤 | 预期结果 | 阻断级 | 状态 |
|---|---------|---------|---------|--------|------|
| IO1 | 采集上报 | Agent 上线后观察详情页 | 上报每磁盘读写速率(byte/s) | 是 | ✅ |
| IO2 | 历史图表 | 详情页历史模式查看磁盘 IO | Read/Write 双折线图,含图例 | 是 | ✅ |
| IO3 | 制造负载 | 主机执行大文件读写 | 图表读写曲线明显上升 | 否 | — |
| IO4 | dashboard widget | 添加 disk-io widget | 渲染读写折线图 | 否 | ✅ |
| IO5 | 不支持平台 | Agent 运行于不支持平台 | 优雅缺省,不报错 | 否 | — |

> ✅ IO2(编排者复核更正,原子代理误报): 详情页 Disk I/O 历史图表存在且正常。设计上 `$id.tsx:531` `hasDiskIo = !isRealtime && diskIoPerDiskData.length>0` —— **仅历史模式**(range=1h/6h/24h/7d/30d)渲染,默认 Real-time 模式不显示(实时模式用其他小图)。子代理在默认 Real-time 模式查看故误判"缺图"。编排者用 `?range=24h` 复验:详情页正确渲染 "Disk I/O" 卡片,含 Merged/Per Disk 双标签、Read/Write 双折线与图例。用例 IO2 步骤明确要求"历史模式",据此判 ✅。
> IO3 —: 共享被测主机,不便制造持续大文件读写负载(会影响其他子代理与宿主机),未执行。
> IO5 —: 本机 macOS 为受支持平台(已采集 disk_io),无不支持平台可验证。

**汇总**:✅ 3 / ❌ 0 / — 2(IO3/IO5 环境限制未执行)
