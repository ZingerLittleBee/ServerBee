# 网络质量页面测试用例

## 前置条件

> 通用环境搭建参照 [README.md](README.md) 中的「启动本地环境」部分。以下为完整独立步骤：

```bash
# 1. 构建并启动 Server
cd apps/web && bun install && bun run build && cd ../..
SERVERBEE_ADMIN__PASSWORD=admin123 SERVERBEE_AUTH__SECURE_COOKIE=false cargo run -p serverbee-server &
sleep 8

# 2. 登录获取 auto-discovery key
curl -s -c /tmp/sb-cookies.txt -X POST http://localhost:9527/api/auth/login \
  -H 'Content-Type: application/json' -d '{"username":"admin","password":"admin123"}'
KEY=$(curl -s -b /tmp/sb-cookies.txt http://localhost:9527/api/settings/auto-discovery-key | python3 -c "import sys,json; print(json.load(sys.stdin)['data']['key'])")

# 3. 启动 Agent
SERVERBEE_SERVER_URL="http://127.0.0.1:9527" SERVERBEE_AUTO_DISCOVERY_KEY="$KEY" cargo run -p serverbee-agent &
sleep 10

# 4. 创建 member 用户（用于权限测试）
curl -s -b /tmp/sb-cookies.txt -X POST http://localhost:9527/api/users \
  -H 'Content-Type: application/json' -d '{"username":"viewer","password":"viewer123","role":"member"}'

# 5. 打开浏览器
agent-browser open http://localhost:9527/login
```

---

## 一、网络质量总览页（/network）

### 1.1 页面加载与基础渲染

| # | 测试场景 | 操作步骤 | 预期结果 | 状态 |
|---|---------|---------|---------|------|
| OV-1 | 页面正常加载 | 登录后导航到 `/network` | 页面加载完成，显示标题和统计卡片 | — |
| OV-2 | 统计卡片数据 | 查看页面顶部统计卡片 | 显示 3 张统计卡片：Total Servers、Online Servers、Anomalies，数值与实际一致 | — |
| OV-3 | 服务器卡片渲染 | 查看服务器网格区域 | 每张卡片显示：服务器名称、在线/离线状态徽章、平均延迟、可用性百分比、目标数量 | — |
| OV-4 | 在线服务器卡片 | 查看已连接 Agent 的服务器卡片 | 状态徽章显示绿色 "Online"，延迟和可用性有具体数值 | — |
| OV-5 | 离线服务器卡片 | 停止 Agent → 等待 60s → 刷新页面 | 状态徽章显示红色 "Offline"，卡片整体透明度降低（opacity 0.7） | — |
| OV-6 | 最差线路提示 | 查看有多个探测目标的服务器卡片 | 如果存在高延迟目标，卡片底部显示琥珀色警告横幅，标注最差线路名称和延迟 | — |
| OV-7 | 异常指示器 | 服务器有异常探测记录时 | 卡片右上方显示红色圆点 + 异常数量 | — |
| OV-8 | 异常告警横幅 | 有服务器存在异常时 | 页面顶部显示琥珀色告警横幅："X server(s) have network anomalies" | — |

### 1.2 搜索过滤

| # | 测试场景 | 操作步骤 | 预期结果 | 状态 |
|---|---------|---------|---------|------|
| OV-9 | 按名称搜索匹配 | 在搜索框输入服务器名称的一部分 | 卡片列表实时过滤，仅显示名称匹配的服务器 | — |
| OV-10 | 搜索大小写不敏感 | 输入全小写关键词 | 匹配任意大小写的服务器名称 | — |
| OV-11 | 搜索无匹配结果 | 输入不存在的名称 | 显示 "No servers match your search" 占位文字 | — |
| OV-12 | 清空搜索 | 清空搜索框 | 恢复显示所有服务器卡片 | — |
| OV-13 | 搜索参数持久化 | 输入搜索词 → 复制 URL → 新窗口打开 | URL 包含 `?q=搜索词`，新窗口中搜索框预填内容且列表已过滤 | — |

### 1.3 导航交互

| # | 测试场景 | 操作步骤 | 预期结果 | 状态 |
|---|---------|---------|---------|------|
| OV-14 | 点击卡片进入详情 | 点击任意服务器卡片 | 跳转到 `/network/:serverId?range=1h` | — |
| OV-15 | 侧边栏导航 | 点击侧边栏 "Network" 链接 | 导航到 `/network` 总览页 | — |

### 1.4 空状态与异常

| # | 测试场景 | 操作步骤 | 预期结果 | 状态 |
|---|---------|---------|---------|------|
| OV-16 | 无服务器空状态 | 删除所有服务器后访问 `/network` | 显示 "No Data" 居中占位，带虚线边框容器 | — |
| OV-17 | 加载状态 | 首次加载或网络延迟较高时 | 显示居中旋转动画 | — |

---

## 二、网络质量详情页（/network/:serverId）

### 2.1 页头信息

| # | 测试场景 | 操作步骤 | 预期结果 | 状态 |
|---|---------|---------|---------|------|
| DT-1 | 返回总览链接 | 点击 "Back to overview" | 返回 `/network` 总览页 | — |
| DT-2 | 服务器标题 | 查看页头 | 显示服务器名称（h1 加粗） + 在线/离线状态徽章 | — |
| DT-3 | 服务器信息栏 | 查看标题下方信息栏 | 条件显示：IPv4、IPv6、地区、OS、上次探测时间（格式 "MMM D, HH:mm"） | — |
| DT-4 | 加载状态 | 页面首次加载 | 居中旋转动画，最小高度 400px | — |
| DT-5 | 服务器不存在 | 访问 `/network/不存在的id` | 显示 "Server not found" 居中文字 | — |

### 2.2 时间范围切换

| # | 测试场景 | 操作步骤 | 预期结果 | 状态 |
|---|---------|---------|---------|------|
| DT-6 | 默认选中 1h | 从总览页点击卡片进入 | "1h" 按钮高亮选中（primary 样式） | — |
| DT-7 | 切换到 Realtime | 点击 "Realtime" 按钮 | URL 更新为 `?range=realtime`，图表切换为实时 WebSocket 流数据，X 轴格式 HH:mm:ss | — |
| DT-8 | 切换到 6h | 点击 "6h" 按钮 | URL 更新为 `?range=6`，图表显示 6 小时历史数据 | — |
| DT-9 | 切换到 24h | 点击 "24h" 按钮 | URL 更新为 `?range=24`，图表显示 24 小时历史数据 | — |
| DT-10 | 切换到 7d | 点击 "7d" 按钮 | URL 更新为 `?range=168`，图表显示 7 天历史数据 | — |
| DT-11 | 切换到 30d | 点击 "30d" 按钮 | URL 更新为 `?range=720`，图表显示 30 天历史数据 | — |
| DT-12 | Realtime → 历史切换 | 在 Realtime 模式下切换到 1h | 图表从实时流切换为历史查询数据，X 轴格式变为 HH:mm | — |
| DT-13 | 历史 → Realtime 切换 | 从 1h 切换到 Realtime | 图表显示 WebSocket 流数据 + 最近 1 小时 seed 数据（图表非空） | — |
| DT-14 | URL 直接指定范围 | 直接访问 `?range=24` | 24h 按钮高亮，图表显示 24 小时数据 | — |
| DT-15 | 无效 range 参数 | 访问 `?range=abc` | 回退到默认 realtime 模式 | — |

### 2.3 目标卡片与显隐切换

| # | 测试场景 | 操作步骤 | 预期结果 | 状态 |
|---|---------|---------|---------|------|
| DT-16 | All Targets 标签页 | 默认查看 | "All Targets" 标签选中，显示所有已配置目标的 TargetCard | — |
| DT-17 | 目标卡片内容 | 查看任意 TargetCard | 显示：颜色圆点、目标名称（截断）、延迟值（等宽字体）、丢包率 | — |
| DT-18 | 新目标（无数据）显示 | 添加新目标但还没有探测数据 | 目标卡片正常显示，延迟为 "N/A" 或 "—"，不报错 | — |
| DT-19 | 隐藏目标 | 点击目标卡片上的眼睛图标 | 图标变为 EyeOff，卡片透明度降低（0.5），图表中对应颜色线条消失 | — |
| DT-20 | 显示目标 | 再次点击已隐藏目标的眼睛图标 | 图标恢复为 Eye，卡片恢复正常透明度，图表中线条重新出现 | — |
| DT-21 | 隐藏所有目标 | 依次隐藏所有目标 | 图表仅显示坐标轴，无数据线 | — |
| DT-22 | 颜色一致性 | 对比卡片颜色圆点与图表线条 | 每个目标卡片的颜色圆点与图表中对应线条颜色一一匹配 | — |
| DT-23 | By Provider 标签页 | 点击 "By Provider" 标签 | 切换为按运营商分组视图，每组显示：运营商名、平均延迟、平均丢包率、目标列表 | — |
| DT-24 | 运营商分组排序 | 查看 By Provider 视图 | 已知运营商（电信/联通/移动/国际）排在前面，其余排后面 | — |

### 2.4 延迟图表

| # | 测试场景 | 操作步骤 | 预期结果 | 状态 |
|---|---------|---------|---------|------|
| DT-25 | 图表正常渲染 | 等待数据加载完成 | AreaChart 渲染，每个可见目标一条彩色填充线（fill opacity 0.05, stroke width 2） | — |
| DT-26 | 图表 X 轴格式（实时） | 切换到 Realtime 模式 | X 轴时间格式为 HH:mm:ss | — |
| DT-27 | 图表 X 轴格式（历史） | 切换到 1h/6h/24h | X 轴时间格式为 HH:mm | — |
| DT-28 | 图表 Tooltip | 鼠标悬停图表数据点 | Tooltip 显示时间戳 + 各目标延迟值（ms） | — |
| DT-29 | 图表无数据状态 | 无探测记录时 | 显示 "No data available" 居中文字（300px 高度容器） | — |
| DT-30 | 实时数据追加 | 在 Realtime 模式等待 2 分钟 | 图表持续追加新数据点，旧数据点向左滚出 | — |
| DT-31 | 数据 60 秒分桶 | 查看图表数据点密度 | 相同 60 秒时间窗口内的多条记录聚合为一个数据点 | — |

### 2.5 底部统计栏

| # | 测试场景 | 操作步骤 | 预期结果 | 状态 |
|---|---------|---------|---------|------|
| DT-32 | 统计数据展示 | 查看图表下方 3 列统计 | 显示：平均延迟（X.X ms）、可用性（X.X%）、目标数量 | — |
| DT-33 | 无数据时显示 N/A | 无探测记录时 | 平均延迟显示 "N/A" | — |

### 2.6 异常摘要表

| # | 测试场景 | 操作步骤 | 预期结果 | 状态 |
|---|---------|---------|---------|------|
| DT-34 | 异常表渲染 | 有异常记录时查看底部 | 表格显示列：Time、Target、Type、Value，标题显示 "Anomalies (N)" | — |
| DT-35 | 严重异常标红 | 查看 unreachable/very_high_latency/very_high_packet_loss 类型 | Type 列使用红色 Badge | — |
| DT-36 | 普通异常标黄 | 查看 high_latency/high_packet_loss 类型 | Type 列使用琥珀色 Badge | — |
| DT-37 | 异常值格式 | 查看 Value 列 | 延迟类异常显示 "X.X ms"，丢包类异常显示 "X.X%" | — |
| DT-38 | 无异常状态 | 所有探测正常时 | 显示 "No anomalies detected" | — |

### 2.7 CSV 导出

| # | 测试场景 | 操作步骤 | 预期结果 | 状态 |
|---|---------|---------|---------|------|
| DT-39 | 导出 CSV | 有数据时点击 "Export CSV" | 浏览器下载文件 `network-{serverId}-{range}.csv` | — |
| DT-40 | CSV 内容正确 | 打开下载的 CSV 文件 | 包含 header: `timestamp,target_id,avg_latency,min_latency,max_latency,packet_loss,packet_sent,packet_received`，数据行与页面一致 | — |
| DT-41 | 无数据时禁用 | 无探测记录时 | "Export CSV" 按钮变灰禁用 | — |

### 2.8 Traceroute

| # | 测试场景 | 操作步骤 | 预期结果 | 状态 |
|---|---------|---------|---------|------|
| DT-42 | 输入目标地址 | 在 Traceroute 输入框输入 IP 或域名 | 输入框接受输入，"Run Traceroute" 按钮变为可点击 | — |
| DT-43 | 空输入禁用按钮 | 输入框为空 | "Run Traceroute" 按钮禁用 | — |
| DT-44 | 执行 Traceroute | 输入目标 → 点击 "Run Traceroute" | 按钮变为旋转图标 + "Running traceroute..."，每 2 秒轮询结果 | — |
| DT-45 | 回车触发执行 | 输入目标 → 按 Enter 键 | 与点击按钮效果相同 | — |
| DT-46 | 结果表格渲染 | Traceroute 完成后 | 表格显示列：Hop、IP、Hostname、RTT1、RTT2、RTT3、ASN | — |
| DT-47 | RTT 颜色编码 | 查看 RTT 列数值 | <50ms 绿色、<100ms 黄色、>=100ms 红色、无响应显示 "*" | — |
| DT-48 | Traceroute 错误 | 对无法到达的地址执行 traceroute | 显示红色边框错误框，内容为错误信息 | — |

---

## 三、管理目标对话框（详情页内）

### 3.1 打开与初始化

| # | 测试场景 | 操作步骤 | 预期结果 | 状态 |
|---|---------|---------|---------|------|
| MT-1 | Admin 可见按钮 | 以 admin 身份打开详情页 | "Manage Targets" 按钮可见 | — |
| MT-2 | Member 不可见按钮 | 以 viewer 身份打开详情页 | "Manage Targets" 按钮不存在 | — |
| MT-3 | 打开对话框 | 点击 "Manage Targets" 按钮 | Dialog 弹出，标题 "Manage Targets"，显示 Select All / Deselect All 按钮 | — |
| MT-4 | 已选目标预勾选 | 打开对话框 | 当前服务器已配置的目标 checkbox 为 checked=true，其余为 unchecked | — |

### 3.2 勾选操作

| # | 测试场景 | 操作步骤 | 预期结果 | 状态 |
|---|---------|---------|---------|------|
| MT-5 | 勾选新目标 | 点击未选中目标的 checkbox | 该项变为 checked | — |
| MT-6 | 取消勾选 | 点击已选中目标的 checkbox | 该项变为 unchecked | — |
| MT-7 | Select All | 点击 "Select All" 按钮 | 所有目标（96 个预设 + 自定义）全部变为 checked | — |
| MT-8 | Deselect All | 点击 "Deselect All" 按钮 | 所有目标变为 unchecked | — |
| MT-9 | 目标列表可滚动 | 查看目标列表区域 | 列表区域最大高度 320px，超出部分可滚动 | — |
| MT-10 | 目标信息展示 | 查看列表中每一项 | 显示：checkbox + 名称 + 运营商（muted）+ 地区（muted）+ 探测类型 Badge（大写） | — |

### 3.3 保存与取消

| # | 测试场景 | 操作步骤 | 预期结果 | 状态 |
|---|---------|---------|---------|------|
| MT-11 | 保存选择 | 勾选 3 个新目标 → 点击 "Save" | toast 显示 "Server targets updated"，对话框关闭，页面刷新显示新目标卡片 | — |
| MT-12 | 保存后重新打开 | 保存 → 再次点击 "Manage Targets" | 之前保存的勾选状态正确恢复（新增目标为 checked） | — |
| MT-13 | 取消不保存 | 勾选几个目标 → 点击 "Cancel" | 对话框关闭，页面目标列表不变 | — |
| MT-14 | 点击遮罩关闭 | 勾选几个目标 → 点击对话框外部遮罩 | 对话框关闭，不保存（与 Cancel 行为一致） | — |
| MT-15 | 最大目标数限制 | 选择超过 20 个目标 → 点击 Save | toast 显示错误信息（422 Validation Error），对话框保持打开 | — |
| MT-16 | 保存空选择 | Deselect All → 点击 Save | 保存成功，页面目标列表变为空 | — |
| MT-17 | 新目标立即显示 | 保存包含之前无数据的新目标 | 新目标卡片在页面立即出现（延迟值为 N/A），不需等待探测数据 | — |

---

## 四、网络探测设置页（/settings/network-probes）

### 4.1 页面访问与权限

| # | 测试场景 | 操作步骤 | 预期结果 | 状态 |
|---|---------|---------|---------|------|
| ST-1 | Admin 可访问 | 以 admin 身份导航到 `/settings/network-probes` | 页面正常加载，显示标题 "Network Probe Settings" | — |
| ST-2 | Member 不可访问 | 以 viewer 身份导航到 `/settings/network-probes` | 重定向或显示无权限提示 | — |
| ST-3 | 侧边栏导航 | 点击侧边栏 "Network Probes" 链接 | 导航到设置页 | — |

### 4.2 目标管理标签页

| # | 测试场景 | 操作步骤 | 预期结果 | 状态 |
|---|---------|---------|---------|------|
| ST-4 | 默认选中 Targets 标签 | 进入设置页 | "Targets" 标签选中，显示目标列表表格 | — |
| ST-5 | 表格列展示 | 查看目标表格 | 列：Name、Provider、Location、Address、Type、Status、Actions | — |
| ST-6 | 预设目标锁定显示 | 查看预设目标行 | Status 列显示锁图标 + "Preset"，Actions 列无编辑/删除按钮 | — |
| ST-7 | 自定义目标显示 | 查看自定义目标行 | Status 列显示 "Custom"，Actions 列有编辑（铅笔）和删除（垃圾桶）按钮 | — |
| ST-8 | 预设目标数量 | 统计列表中预设目标 | 共 96 个预设目标（电信 31 + 联通 31 + 移动 31 + 国际 3） | — |

### 4.3 添加自定义目标

| # | 测试场景 | 操作步骤 | 预期结果 | 状态 |
|---|---------|---------|---------|------|
| ST-9 | 打开添加对话框 | 点击 "Add Target" 按钮 | Dialog 弹出，标题 "Add Target"，表单字段为空 | — |
| ST-10 | 填写必填字段 | 填写 Name 和 Address | 提交按钮可用 | — |
| ST-11 | 缺少必填字段提交 | 仅填 Name（不填 Address）→ 提交 | 表单验证失败，不发送请求 | — |
| ST-12 | 选择探测类型 | 点击 Probe Type 下拉 | 选项：ICMP (Ping)、TCP、HTTP，默认 ICMP | — |
| ST-13 | 创建成功 | 填写完整信息 → 点击 "Add Target" | toast "Target created"，对话框关闭，列表末尾出现新目标（Status: Custom） | — |
| ST-14 | 创建失败 | 填写重复名称 → 提交 | toast 显示错误信息 | — |

### 4.4 编辑自定义目标

| # | 测试场景 | 操作步骤 | 预期结果 | 状态 |
|---|---------|---------|---------|------|
| ST-15 | 打开编辑对话框 | 点击自定义目标的编辑按钮 | Dialog 弹出，标题 "Edit Target"，表单预填当前值 | — |
| ST-16 | 修改并保存 | 修改名称 → 点击 "Save" | toast "Target updated"，列表中名称更新 | — |
| ST-17 | 预设目标不可编辑 | 查看预设目标行 | 无编辑按钮（不可触发编辑） | — |
| ST-18 | 编辑预设目标 API | 直接调用 `PUT /api/network-probes/targets/{preset-id}` | 返回 403 Forbidden | — |

### 4.5 删除目标

| # | 测试场景 | 操作步骤 | 预期结果 | 状态 |
|---|---------|---------|---------|------|
| ST-19 | 打开删除确认 | 点击自定义目标的删除按钮 | 弹出确认对话框："Are you sure you want to delete this target?" | — |
| ST-20 | 确认删除 | 点击 "Delete" 确认按钮 | toast "Target deleted"，目标从列表消失 | — |
| ST-21 | 取消删除 | 点击 "Cancel" 按钮 | 对话框关闭，目标仍在列表 | — |
| ST-22 | 预设目标不可删除 | 查看预设目标行 | 无删除按钮 | — |
| ST-23 | 删除预设目标 API | 直接调用 `DELETE /api/network-probes/targets/{preset-id}` | 返回 403 Forbidden | — |
| ST-24 | 删除级联效果 | 删除一个已分配给某服务器的自定义目标 | 目标从所有服务器配置中移除，Agent 收到同步通知 | — |

### 4.6 全局设置标签页

| # | 测试场景 | 操作步骤 | 预期结果 | 状态 |
|---|---------|---------|---------|------|
| ST-25 | 切换到设置标签 | 点击 "Settings" 标签 | 显示全局设置表单（URL 更新为 `?tab=settings`） | — |
| ST-26 | 探测间隔字段 | 查看 Probe Interval 输入框 | 显示当前值，范围 30-600 秒，帮助文字说明范围 | — |
| ST-27 | 探测包数字段 | 查看 Packets per Round 输入框 | 显示当前值，范围 5-20，帮助文字说明范围 | — |
| ST-28 | 默认目标列表 | 查看 Default Targets 区域 | 可滚动 checkbox 列表（最大高度 192px），每项显示目标名 + 探测类型 | — |
| ST-29 | 修改间隔并保存 | 改为 120 → 点击 Save | toast "Settings saved" | — |
| ST-30 | 刷新后持久化 | 保存后刷新页面 | 间隔值仍为 120 | — |
| ST-31 | 间隔下限验证 | 输入 10 → 保存 | 验证失败或 API 返回错误（最小 30） | — |
| ST-32 | 间隔上限验证 | 输入 999 → 保存 | 验证失败或 API 返回错误（最大 600） | — |
| ST-33 | 勾选默认目标 | 勾选 3 个默认目标 → 保存 → 新服务器注册 | 新服务器自动分配这 3 个目标 | — |

---

## 五、实时数据流（WebSocket）

| # | 测试场景 | 操作步骤 | 预期结果 | 状态 |
|---|---------|---------|---------|------|
| WS-1 | 实时数据接收 | 详情页 Realtime 模式 → 等待 | 图表每个探测周期追加新数据点 | — |
| WS-2 | 实时数据滚动窗口 | 长时间保持 Realtime 模式 | 每个目标最多保留 200 个数据点，旧数据自动裁剪 | — |
| WS-3 | Seed 数据加载 | 切换到 Realtime 模式 | 图表立即显示最近 1 小时历史数据作为 seed，然后叠加实时数据 | — |
| WS-4 | 数据去重 | Seed 数据与实时数据有重叠时间戳 | 图表中不出现重复数据点 | — |
| WS-5 | 总览页自动刷新 | 在 `/network` 页面等待 60 秒 | 服务器卡片数据自动刷新（refetchInterval 60s） | — |

---

## 六、权限控制

| # | 测试场景 | 操作步骤 | 预期结果 | 状态 |
|---|---------|---------|---------|------|
| AC-1 | Admin 完整权限 | 以 admin 登录 → 访问所有网络页面 | 所有页面可访问，所有操作可执行 | — |
| AC-2 | Member 只读访问 | 以 viewer 登录 → 访问 `/network` | 总览页正常显示，无写入操作按钮 | — |
| AC-3 | Member 详情页只读 | 以 viewer 登录 → 访问 `/network/:id` | 详情页正常显示，无 "Manage Targets" 按钮 | — |
| AC-4 | Member 设置页拦截 | 以 viewer 登录 → 访问 `/settings/network-probes` | 被拦截或重定向 | — |
| AC-5 | Member 写入 API 拦截 | 以 viewer 身份调用 `PUT /api/servers/:id/network-probes/targets` | 返回 403 | — |
| AC-6 | 未认证访问 | 未登录直接访问 `/network` | 重定向到 `/login` | — |

---

## 七、国际化（i18n）

| # | 测试场景 | 操作步骤 | 预期结果 | 状态 |
|---|---------|---------|---------|------|
| I18N-1 | 切换中文 | 点击页头 "中文" 按钮 | 全站切换为中文 | — |
| I18N-2 | 总览页中文 | 中文模式下查看 `/network` | 标题/统计卡片/状态徽章/搜索框/空状态全部中文 | — |
| I18N-3 | 详情页中文 | 中文模式下查看详情页 | 时间范围按钮、目标标签、统计标签、异常表头、Traceroute 全部中文 | — |
| I18N-4 | 设置页中文 | 中文模式下查看设置页 | 标签名、表格列头、对话框标题、表单标签、帮助文字全部中文 | — |
| I18N-5 | 运营商名中文 | 中文模式下查看 By Provider 分组 | 显示：电信、联通、移动、国际、其他 | — |
| I18N-6 | 切回英文 | 点击 "EN" 按钮 | 全站恢复英文 | — |
| I18N-7 | 语言持久化 | 切换语言 → 刷新页面 | 语言保持不变（localStorage） | — |

---

## 八、边界条件与异常场景

| # | 测试场景 | 操作步骤 | 预期结果 | 状态 |
|---|---------|---------|---------|------|
| EC-1 | 单目标服务器 | 仅配置 1 个目标 → 查看详情页 | 图表显示单条线，目标卡片 1 个，By Provider 仅 1 组 | — |
| EC-2 | 最大目标数 | 为服务器配置 20 个目标 → 查看详情页 | 20 个目标卡片正常渲染，图表 20 条线条可区分（10 色循环） | — |
| EC-3 | 超过最大目标数 | 尝试配置 21 个目标 | API 返回 422 错误 | — |
| EC-4 | 100% 丢包 | 目标完全不可达时 | 卡片延迟显示 "N/A"，图表线条中断，异常表出现 "unreachable" 记录 | — |
| EC-5 | Agent 离线再上线 | 停止 Agent → 查看详情 → 重启 Agent | 离线期间数据停止更新 → 上线后实时数据恢复推送 | — |
| EC-6 | 高延迟目标 | 目标延迟 >150ms | 异常表出现 "high_latency" 记录，总览卡片可能出现异常指示器 | — |
| EC-7 | 服务器名称很长 | 服务器名称超过 20 字符 | 总览卡片名称截断显示，详情页标题完整显示 | — |
| EC-8 | 浏览器标签页切换 | 切换到其他标签页 → 切回 | WebSocket 连接保持，数据无遗漏 | — |
| EC-9 | 并发修改目标 | 两个 admin 同时修改同一服务器目标 | 后提交的覆盖前者（最终一致性），无报错 | — |
| EC-10 | 刷新后实时模式 | Realtime 模式下按 F5 刷新 | 页面重新加载，seed 数据填充图表，实时数据继续追加 | — |
| EC-11 | 快速切换时间范围 | 连续快速点击 Realtime → 1h → 6h → 24h | 每次切换都正确加载对应数据，无请求错乱或数据串用 | — |
| EC-12 | 延迟颜色边界值 | 目标延迟恰好为 50ms / 100ms | 50ms 显示黄色（>=50），100ms 显示红色（>=100），0ms 显示绿色 | — |
| EC-13 | 超大延迟值 | 目标延迟 >1000ms | 数值完整显示（如 "1234.5 ms"），不溢出或截断 | — |
| EC-14 | Traceroute 多次执行 | 执行完一次 traceroute → 修改目标 → 再次执行 | 结果正确更新为新目标的路由，旧结果被替换 | — |
| EC-15 | 无 IPv4/IPv6 服务器 | 服务器无 IP 信息 | 信息栏中 IPv4/IPv6 字段不显示，不报错 | — |

---

## 九、异常检测阈值（后端逻辑）

> 以下测试验证后端 `get_anomalies` 的分类逻辑，可通过 API 直接调用
> `GET /api/servers/{id}/network-probes/anomalies?from=...&to=...` 验证。

| # | 测试场景 | 条件 | 预期异常类型 | 状态 |
|---|---------|------|-------------|------|
| AN-1 | 完全不可达 | `packet_loss == 1.0` | `unreachable` | — |
| AN-2 | 极高延迟 | `avg_latency > 240ms` | `very_high_latency` | — |
| AN-3 | 高延迟 | `150ms < avg_latency <= 240ms` | `high_latency` | — |
| AN-4 | 正常延迟 | `avg_latency <= 150ms` | 无异常 | — |
| AN-5 | 极高丢包 | `packet_loss > 50%` | `very_high_packet_loss` | — |
| AN-6 | 高丢包 | `10% < packet_loss <= 50%` | `high_packet_loss` | — |
| AN-7 | 正常丢包 | `packet_loss <= 10%` | 无异常 | — |
| AN-8 | 延迟+丢包同时异常 | `avg_latency=200ms, packet_loss=30%` | 同时产生 `high_latency` 和 `high_packet_loss` 两条记录 | — |
| AN-9 | 不可达优先 | `packet_loss=100%`（此时 latency 可能为 null） | 仅产生 `unreachable`，不再额外产生丢包异常 | — |
| AN-10 | 异常窗口 24h | 异常发生在 25 小时前 | summary 中 `anomaly_count` 不计入此条 | — |

---

## 十、历史记录查询策略（后端逻辑）

> 后端 `query_records` 根据时间跨度自动选择数据粒度。

| # | 测试场景 | 操作步骤 | 预期结果 | 状态 |
|---|---------|---------|---------|------|
| QR-1 | 短时间范围返回原始记录 | `GET /api/servers/{id}/network-probes/records?from=1h_ago&to=now` | 返回原始探测记录（每条为单次探测） | — |
| QR-2 | 长时间范围返回小时聚合 | `GET /api/servers/{id}/network-probes/records?from=2d_ago&to=now` | 返回小时聚合记录（每目标每小时一条） | — |
| QR-3 | 切换点为 1 天 | 时间跨度恰好 24h | 返回原始记录（< 1 day 判定） | — |
| QR-4 | 聚合字段正确 | 查看 7d 数据的单条记录 | `avg_latency` 为该小时平均值，`packet_loss` 为该小时平均丢包率 | — |

---

## 十一、Agent 同步通知

| # | 测试场景 | 操作步骤 | 预期结果 | 状态 |
|---|---------|---------|---------|------|
| AS-1 | 修改目标触发同步 | 通过 Manage Targets 保存新目标 → 查看 Agent 日志 | Agent 收到 `NetworkProbeSync` 消息，开始探测新目标 | — |
| AS-2 | Agent 在线时即时同步 | Agent 在线 → 修改目标 | Agent 立即收到配置更新，下一轮探测使用新目标列表 | — |
| AS-3 | Agent 离线时不报错 | Agent 离线 → 修改目标 | Server 端保存成功（返回 200），不因 Agent 离线而失败 | — |
| AS-4 | Agent 重连后同步 | Agent 离线期间修改目标 → Agent 重连 | Agent 重连后收到 Welcome + 最新目标配置 | — |
| AS-5 | 全局设置同步 | 修改探测间隔 → 查看 Agent 行为 | Agent 使用新间隔执行探测 | — |

---

## 十二、管理目标对话框 — 空目标状态

| # | 测试场景 | 操作步骤 | 预期结果 | 状态 |
|---|---------|---------|---------|------|
| MT-18 | 无可用目标 | 删除所有自定义目标 + 无预设时打开对话框 | 显示 "No probe targets configured" 居中文字，Save 按钮仍可点击（保存空列表） | — |

---

## 十三、By Provider 分组视图

| # | 测试场景 | 操作步骤 | 预期结果 | 状态 |
|---|---------|---------|---------|------|
| PV-1 | 分组卡片渲染 | 详情页 → 点击 "By Provider" 标签 | 每个运营商一张 Card，标题为运营商名，显示平均延迟和平均丢包 | — |
| PV-2 | 运营商内目标列表 | 查看某运营商卡片内部 | 列表显示该运营商下每个目标的名称、延迟（颜色编码）、丢包率 | — |
| PV-3 | 全部目标无数据 | 某运营商下所有目标均无探测记录 | 平均延迟显示 "N/A"（avgLatency 返回 null） | — |
| PV-4 | 部分目标有数据 | 运营商下 3 个目标，仅 1 个有数据 | 平均延迟仅计算有数据的目标（跳过 null） | — |
| PV-5 | 运营商排序 | 查看分组顺序 | 已知运营商（电信→联通→移动→国际）在前，自定义运营商在后 | — |

---

## 测试统计

| 模块 | 用例数 |
|------|--------|
| 总览页 | 17 |
| 详情页 | 38 |
| 管理目标对话框 | 18 |
| 设置页 | 33 |
| 实时数据流 | 5 |
| 权限控制 | 6 |
| 国际化 | 7 |
| 边界条件 | 15 |
| 异常检测阈值 | 10 |
| 历史记录查询策略 | 4 |
| Agent 同步通知 | 5 |
| By Provider 分组视图 | 5 |
| **合计** | **163** |
